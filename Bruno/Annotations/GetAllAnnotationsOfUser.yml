info:
  name: GetAllAnnotationsOfUser
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:5001/api/annotations/user
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
