info:
  name: GetUser
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:5001/api/appuser?id=2
  params:
    - name: id
      value: "2"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
