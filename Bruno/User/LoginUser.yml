info:
  name: LoginUser
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:5001/api/auth/tokens
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "Username": "mazeettest",
        "Password": "testtest"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
